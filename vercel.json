{
  "version": 2,
  "builds": [
    {
      "src": "apps/admin/next.config.js",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install",
        "buildCommand": "next build"
      }
    }
  ]
}