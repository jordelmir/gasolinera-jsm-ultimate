{
  "name": "gasolinera-jsm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "vercel-build": "npx nx build admin --prod && npx nx build advertiser --prod",
    "start": "nx serve",
    "build": "nx build",
    "test": "nx test",
    "nx": "nx"
  },
  "devDependencies": {
    "@nx/next": "^21.4.0",
    "@nx/workspace": "^21.4.0",
    "nx": "^21.4.0",
    "typescript": "~5.9.2"
  }
}
