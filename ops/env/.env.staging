# ========================================================
# STAGING - Gasolinera JSM Ultimate
# ========================================================
SPRING_PROFILES_ACTIVE=staging
NODE_ENV=production

# ========================================================
# BASE DE DATOS - STAGING
# ========================================================
POSTGRES_HOST=${POSTGRES_HOST}
POSTGRES_PORT=5432
POSTGRES_DB=puntog_staging
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# ========================================================
# CACHE Y MESSAGING - STAGING
# ========================================================
REDIS_URL=${REDIS_URL}
RABBITMQ_HOST=${RABBITMQ_HOST}
RABBITMQ_PORT=5672
RABBITMQ_USER=${RABBITMQ_USER}
RABBITMQ_PASS=${RABBITMQ_PASS}

# ========================================================
# KAFKA - STAGING
# ========================================================
KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}

# ========================================================
# SECRETOS - STAGING (USAR VAULT O SECRETS MANAGER)
# ========================================================
JWT_SECRET=${JWT_SECRET}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
QR_SIGNATURE_SECRET=${QR_SIGNATURE_SECRET}
QR_PUBLIC_KEY=${QR_PUBLIC_KEY}

# ========================================================
# VAULT - STAGING
# ========================================================
VAULT_TOKEN=${VAULT_TOKEN}
VAULT_HOST=${VAULT_HOST}
VAULT_PORT=8200

# ========================================================
# OBSERVABILIDAD - STAGING
# ========================================================
OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT}
JAEGER_ENDPOINT=${JAEGER_ENDPOINT}

# ========================================================
# CONFIGURACIÓN ESPECÍFICA POR SERVICIO
# ========================================================

# --- Redemption Service ---
GEOFENCE_RADIUS_METERS=50

# --- Ad Engine ---
AD_FALLBACK_URL=${AD_FALLBACK_URL}

# --- Java Options ---
JAVA_OPTS=-Xmx1g -Xms512m -Dspring.profiles.active=staging

# ========================================================
# FRONTEND URLs - STAGING
# ========================================================
NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL}
ADMIN_URL=${ADMIN_URL}
ADVERTISER_URL=${ADVERTISER_URL}
OWNER_DASHBOARD_URL=${OWNER_DASHBOARD_URL}