{
  "name": "gasolinera-jsm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "vercel-build": "npx nx build admin-dashboard --prod && npx nx build advertiser-portal --prod",
    "start": "nx serve",
    "build": "nx build",
    "test": "nx test",
    "nx": "nx"
  },
  "devDependencies": {
    "@nx/workspace": "^19.0.0",
    "nx": "^19.0.0",
    "typescript": "~5.4.0"
  }
}
